rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Presentation/demo mode: keep uploads and downloads unblocked.
    // Tighten these rules again before using real student data in production.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
