rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Presentation/demo mode: keep all app workflows unblocked.
    // Tighten these rules again before using real student data in production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
